6. Loops, Loops, Merry-go-round

Create an array of your friends.
Now, create a loop that will pop a friend out every time the loop runs.
Tell a funny story about how they leave each time. console.log is your friend here.

// ??????

var myBFFs = ['Chiny', 'Oredola', 'Gina', 'Meghan'];
// for loop
 for (var in myBFFs) {
   console.log(myBffs[0] + " " + 'left for Jamaica.');
   console.log(myBffs[1] + " " + 'lives in NYC.');
 }





var myBFFs = ['Chiny', 'Oredola', 'Gina', 'Meghan'];

// for-in loop
 for (i = 0; i < myBFFs.length; i++); {

  if (myBFFs[i] == 'Chiny') {
    break;
    console.log([i] + ' ' + 'left for Jamaica.');
  }

  else (myBffs[1] == 'Oredola') {
    break;
    console.log([i] + ' ' + 'left for NYC.');
  }
}
