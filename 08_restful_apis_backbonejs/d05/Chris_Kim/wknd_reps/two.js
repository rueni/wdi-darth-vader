// 2. Array Manipulation
//
// Create an array.

var array = ['Tokyo', 'Rome', 'Prague']; // Add 3 places you have visited in your life.
var splice = array.splice(0, 2); // Remove two of them from the front of the array.
var push = array.push("Lisbon", "Stockholm", "Amsterdam"); // Add 3 places that you would like to travel to.
var pop = array.pop(); // Remove one of them from the end of the array.
