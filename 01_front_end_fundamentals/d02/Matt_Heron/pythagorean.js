side = [];
var side[0] = prompt("Enter the length of side one");
var side[1] = prompt("Enter the length of the second side");
var hypotenuse = Math.sqrt(Math.pow(side[0], 2) + Math.pow(side[1], 2));
console.log(hypotenuse);
