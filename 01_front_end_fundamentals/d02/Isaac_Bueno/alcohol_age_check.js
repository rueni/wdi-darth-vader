    var name = "Isaac";
    console.log(name);

    //if (name != "Isaac") {
    //alert("GTFO");
  //} else {
    //alert("did you bring the beer?");
  //}


var age = prompt("how old are you?");
if (age >= 21) {
  prompt("get the hell in here and lets do some business");
}else if (age > 18 && age < 21) {
  prompt("come back when you're 21. You're close but not quite ready to do business with us");
}else if (age < 18) {
  prompt("get the hell our of here!");
} else {
  prompt("Not a valid input");
}
