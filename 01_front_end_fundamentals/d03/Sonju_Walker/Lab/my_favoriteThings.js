var my_favs = ["dance", "futbol", "diving", "yoga"];

var index;

for (index in my_favs) {
  console.log("my index is..." + index);
  console.log(my_favs[index]);
}

for (var inc = 0; inc < my_favs.length; inc++){
  console.log(my_favs[inc]);
}
