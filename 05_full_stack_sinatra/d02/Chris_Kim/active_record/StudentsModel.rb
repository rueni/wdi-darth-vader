class Students < ActiveRecord::Base

end

# This creates the class Students which inherits from the ActiveRecord::Base module method.
# It effectively converts the SQL table with the name Students into a Ruby object!
