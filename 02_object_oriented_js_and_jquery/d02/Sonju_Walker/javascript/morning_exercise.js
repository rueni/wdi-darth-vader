Object // can be a ability or attribute which is used with in a function
var str = "My work is good" // string Object
var shoes = ["heels", "flats"] // array Object


Number (including integers and floating point numbers) //  numbers with in an object can be a string or arry
3.12
2
-20

String // strings always have " " around them and are objects they can words or numbers
var num = ("4", "5", "6")
var names = ("Mark", "Angies", "Sonju")
var points =["LA", "0", "NYC", "89"]

String Concatenation // join words, numbers or variables to create a string
var newSen = ("Jerry" + " " + age + + " " +"is not known" + ".");
var cities = ("I like" + " " + city + ".");

*what is parseint, and how is it used?* // The parseInt() function parses a string and returns an integer
var num1 = prompt("give me a number");
// when prompt came up entered 5 and returned "5"

var num2 = prompt("Give me another number");

parseInt(num1) + parseInt(num2) // takes first of two numbers and returns an integer

Boolean // if, else, else if statments that are used to determine truthy or falsey functions
var age = 12 {

  if {
  return  age >= 21;
  console.log("You can have a beer!")
  }
  else {
    console.log("You are underage to drink")
  }

};

Modulus // In computing, the modulo operation finds the remainder after division of one number by another (sometimes called modulus).

10 % 3;
50 % 3;
62 % 5;

Array // index of objects with [ ] not ()
var shoes = ["flats", "heels"];
var starbucks = ["coffee", "lates", "fracpuccino"];

Variable // used to define objects
var cities = ("NYC", "Chicago", "Utah");
var age = [10, 45, 9, 4, 28];
