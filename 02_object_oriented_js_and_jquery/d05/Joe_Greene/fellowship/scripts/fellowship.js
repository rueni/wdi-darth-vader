console.log("Linked.");

// Dramatis Personae
var hobbits = [
  'Frodo Baggins',
  'Samwise \'Sam\' Gamgee',
  'Meriadoc \'Merry\' Brandybuck',
  'Peregrin \'Pippin\' Took'
];

var buddies = [
  'Gandalf the Grey',
  'Legolas',
  'Gimli',
  'Strider',
  'Boromir'
];

var lands = ['The Shire', 'Rivendell', 'Mordor'];
var body = document.body;


// create a section tag with an id of `middle-earth`
makeMiddleEarth = function () {
  var $middle-earth = $('<section>');
  $section.attr("id","middle-earth").append

}





  $("middle-earth" = mid  )          .append(index)

  for (var i = 0; i < array.length; i++) {
    array[i]
  }

  var makeMiddleEarth = function () {
   // create a section tag with an id of `middle-earth`
   // add each land as an `article` tag
   // inside each `article` tag include an `h1` with the name of the land
   // append `middle-earth` to your document `body`
};

makeMiddleEarth();


 middle-earth = <section id="middle-earth"></section>;
 The Shire =  <article><h1>The Shire</h1></article>;
 Rivendell =  <article><h1>Rivendell</h1></article>;
 <article><h1>Mordor</h1></article>;




  </section>

  };

makeMiddleEarth();

$(selector).append(content,function(index,html))




   var makeHobbits = function () {
      // display an `unordered list` of hobbits in the shire
         // (which is the second article tag on the page)
      // give each hobbit a class of `hobbit`
      ul tag .eq
      //anytime for an array or object you can use eq


   };



   var keepItSecretKeepItSafe = function () {

     /just giving the div ring to the li frodo


   // create a div with an id of `'the-ring'`
   // give the div a class of `'magic-imbued-jewelry'`
   // add the ring as a child of `Frodo`
};



var makeBuddies = function () {
   // create an `aside` tag
   // attach an `unordered list` of the `'buddies'` in the aside
   // insert your aside as a child element of `rivendell`



};
